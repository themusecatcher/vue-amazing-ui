# 构建产物（虽已在 .gitignore，此处显式声明，避免依赖 Prettier 的 gitignore 回退行为）
dist/
es/
lib/
dist-ssr/

# 测试覆盖率
coverage/

# 依赖目录
node_modules/

# 锁文件：由 pnpm 生成，格式化会重排其结构，禁止纳入
pnpm-lock.yaml

# unplugin-vue-components 自动生成，内容随扫描时序抖动
components.d.ts

# VitePress 缓存与构建输出
docs/.vitepress/cache/
docs/.vitepress/dist/
